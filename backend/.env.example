# Database connection string for PostgreSQL
DATABASE_URL=postgresql://username:password@localhost:5432/movex_transfer

# Resend API key for sending emails
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Owner email for internal notifications
OWNER_EMAIL=owner@example.com

# Email sender configuration (optional)
FROM_EMAIL=rezervace@movextransfer.cz
FROM_NAME=Movex Transfer
SYSTEM_EMAIL=system@movextransfer.cz

# Port for the server (optional, defaults to 3000)
PORT=3000

# Frontend URL for CORS (comma-separated for multiple origins)
FRONTEND_URL=http://localhost:3000,https://your-domain.railway.app
